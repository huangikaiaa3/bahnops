# Infra

This folder is for later weeks once the live data service is working.

Likely contents:
- `Dockerfile`
- `docker-compose.yml`
- `k8s/`
- `terraform/`

The priority is still ingestion, persistence, and service design first.
